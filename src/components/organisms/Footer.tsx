// src/components/organisms/Header.tsx
import Button from '../atoms/Button';

const Footer = () => {
    return (
        <footer>
            {/* ... 다른 UI 요소 */}
            <Button theme="catchPhrase" onClick={() => {}}>로그인</Button>
        </footer>
    );
};

export default Footer;